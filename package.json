{
  "scripts": {
    "start": "live-server --verbose --no-browser --port=35729 --host=127.0.0.1 --no-css-inject --wait=1000 ./code",
    "start:network": "live-server --verbose --no-browser --port=35729 --host=0.0.0.0 --no-css-inject --wait=1000 ./code"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
